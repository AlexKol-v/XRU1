# Соответствие учебному заданию

Какие оставшиеся ДЗ курса зачитываются финальным проектом XRU1, а какие дешевле
доделать в учебном проекте `UE-HW` (`UE_Learning_TopDown.uproject`, лежит рядом с
XRU1 — точный путь зависит от машины, см. `CLAUDE.md`).

Состояние сверено с фактическим содержимым репозитория и Asset Registry. Даты в
таблицах — даты приёмки конкретного ДЗ: это учебная отчётность, а не история
правок.

## 1. Итоговая таблица

| ДЗ | Тема | Готовность в XRU1 | Где закрывать | Что осталось |
|---|---|---|---|---|
| 1 | Common UI | 4 из 5 критериев | **UE-HW — сдано** | ничего |
| 2 | Система квестов. Часть 3 (NPC) | — | **UE-HW — закрыто** (подтверждено 2026-08-06) | ничего |
| 3 | Пост-эффекты | **закрыто, приёмка пройдена 2026-08-06** (Landscape + постпроцесс + PCG-граф) | **XRU1** | ничего |
| 4 | Звук + 3 Niagara VFX | **закрыто, приёмка пройдена 2026-08-07** (звук, 3 свои системы + 2 привязки набора, радио-обработка) | **XRU1** | ничего |
| 5а | Локализация | **закрыто, приёмка пройдена 2026-08-06** | **XRU1** | ничего |
| 5б | Многопоточность (`AsyncTask`) | 0 % | **UE-HW** | параллельная обработка в Системе квестов |
| — | Проектная работа | критерии закрыты | **XRU1** | упаковка билда (§7) |

**Принцип разделения.** В XRU1 уходит то, что совпадает с работой, которую и так
надо делать; в UE-HW — то, что требует ломать принятую архитектуру XRU1 или трогать
код, рискованный перед защитой.

## 2. ДЗ 1 — Common UI

**Критерии:** базовый класс персонажа; основной персонаж на его основе + расширяемое
управление; ≥3 кастомных анимации; система взаимодействия с объектами сцены,
расширенная 3 новыми категориями; визуальные подсказки на CommonUI.

**В UE-HW закрыто полностью.** XRU1 независимо перекрывает 4 критерия из 5:

| Критерий | Чем закрыт в XRU1 |
|---|---|
| Базовый класс персонажа | `AUnitBase` + донорская иерархия `ABaseCharacter → AGASCharacter → ATDCombatant` |
| Основной персонаж + управление | `AUnit_Assault/Sniper/Healer/Tank`, `ATacticalPlayerController`, Enhanced Input |
| ≥3 кастомных анимации | 5 монтажей (`AM_Fire_Open`, `AM_Fire_OverCover`, `AM_HitReact`, `AM_Death`, `AM_Overwatch_Enter`) + `ABP_Solider` |
| CommonUI-подсказки | стек `WBP_PrimaryGameLayout`, слой субтитров, Slate-трекер целей обучения, попапы POI, полный цикл экранов |
| 3 новые категории интеракции | **не закрыт** |

Про непокрытый критерий: донорские `IInteractable` и `UInteractionDetectorComponent`
не использовались в XRU1 ни одним классом — `ABombObjective`, `AEvacZone`,
`ATacticalQuestZone`, `AHubPOIMarker` сделаны собственной механикой, мимо
интерфейса, — и при чистке кода перед сдачей были удалены из модуля.

**Решение: не трогать.** Задание зачтено учебным проектом.

## 3. ДЗ 2 — Система квестов. Часть 3 (NPC) — **ЗАКРЫТО в UE-HW** (2026-08-06)

**Критерии:** два типа NPC (враг, нейтральный) на базовом классе из модуля 3;
Behavior Tree или StateTree для поведения каждого; два квестовых сценария с
взаимодействием с нейтральным и вражеским NPC. Выполнено в UE-HW на его
BT/ST-инфраструктуре; ниже — историческое обоснование, почему не в XRU1.

**Состояние XRU1:** враг есть (`BP_Unit_Marauder` + `BP_AIController_Marauder`);
нейтрального NPC и фракции «нейтрал» нет; поведение — **utility-скоринг на C++**,
Behavior Tree в проекте **ноль**, StateTree применяется только для квестов; два
квестовых сценария формально есть, но взаимодействия с нейтральным NPC в них нет.

**Почему не в XRU1.** Требование «используйте Behavior Tree или State Tree» XRU1 не
выполняет по существу, а переводить боевой AI на BT ради ДЗ — значит ломать принятую
и отлаженную архитектуру ([04_AI.md](04_AI.md) §1 объясняет, почему выбран не BT)
прямо перед защитой курсовой.

**Почему в UE-HW дёшево.** Инфраструктура там стоит целиком: `BT/` с задачами и
сервисами (`BT_HostileNPC`, `BB_HostileNPC`), `ST/` с 7 задачами и 3 условиями,
`NPC/` с `NPCCharacter`/`NPCAIController`/`NPCDefinition`, готовые `BP_HostileNPC`,
`BP_NeutralNPC`, `BP_FriendlyNPC` и несколько квестов. Своего останется: два
собственных квестовых сценария и свои профили поведения для двух типов.

**Опция, если захочется закрыть в XRU1** (не рекомендуется): добавить нейтрального
NPC — гражданского или инженера, которого надо вывести к `AEvacZone`, — и повесить
на него отдельный StateTree (Idle → Follow → Flee). Это укладывается в лор и даёт
«два типа NPC, у каждого своё поведение», но поведение врага всё равно останется
utility-C++.

## 4. ДЗ 3 — Пост-эффекты (Landscape, Foliage, постпроцессы)

**Критерии:** карта с использованием Landscape и Foliage (и/или PCG); постпроцесс,
меняющий отображение карты.

| Что | Есть? | Факты |
|---|---|---|
| Landscape | **да** | `Main_Map_Showreel` содержит landscape-компоненты и 43 `LandscapeMaterialInstanceConstant`; кит `MWLandscapeAutoMaterial` |
| Постпроцесс | **да, закрыт** | `M_PP_FogOfWar` (туман войны, `Scene Color Before DOF`) и `M_OutlinePP` (обводка) — оба blendable вешаются на `ATacticalCameraPawn` в `BeginPlay` |
| Foliage | нет | ни одного `InstancedFoliageActor` на картах проекта |
| PCG | частично | C++-ноды `PCGScatterJitter`, `PCGSlopeHeightFilter` есть, PCG-графов-ассетов нет |

Постпроцесс-волюмов на картах нет, но критерий формулируется как «постпроцесс,
меняющий отображение карты», и визуальный слой тумана делает это буквально, причём
программно управляемым материалом.

**PCG-граф.** `PCG_DebrisScatter`
(`Content/XRU1Game/PCG/`) построен на СВОИХ C++ нодах проекта: Get Landscape Data →
Surface Sampler → **Slope/Height Filter** (`UPCGSlopeHeightFilterSettings`) →
**Scatter Jitter** (`UPCGScatterJitterSettings`) → Static Mesh Spawner
(SM_CardBoards01). На `Main_Map_Showreel` стоит `PCGVolume` «PCG_DebrisScatter» по
границам ландшафта; генерация даёт ~120 инстансов обломков. Референс — донорский
`Content/PCG/PCG_FoliageScatter` (те же ноды в графе курса).
⚠️ Правило, без которого граф ломает бой: **декор обязан быть прозрачным для
трейсов**. Объём и меши с коллизией перехватывают LOS-трейсы тумана войны, и карта
«слепнет». Поэтому коллизия выключена и у мешей спавнера
(`UXRU1PcgAuthoringLibrary::DisableSpawnerCollision`), и у самого `PCGVolume`.

## 5. ДЗ 4 — Звук и 3 Niagara VFX

**Критерии:** ≥3 разных Niagara VFX, привязанных к событиям сцены/сценария; звуки
персонажа (шаги, дыхание); общий фоновый звук окружения.

**Звук закрыт с запасом:** `UTacticsAudioSubsystem` как единственная точка
воспроизведения, 5 SoundClass, громкости из `UTacticsUserSettings`; шаги с нотифаями
по реальным контактам стопы; реакции боли и усилия; импакты по поверхностям; музыка
меню/хаба/боя + стингеры победы и поражения; 20+ реплик озвучки; профили
`DA_UnitAudio_*` с наследованием от `DA_UnitAudio_Common`.

**Оба пункта закрыты 2026-08-06:**

1. **Niagara-системы и слой MissionVfx.** Написан C++ слой по образцу звукового
   (`UMissionVfxSubsystem` + `UMissionVfxDataAsset`, `Source/XRU1/FX/`): эффекты
   спавнятся по ПОДТВЕРЖДЁННЫМ квест-событиям, не из BP. Привязки в
   `Data/Core/DA_MissionVfx` (назначен в `BP_TacticsGameInstance.MissionVfx`):

   | Событие | Система |
   |---|---|
   | `Ability.Heal.Normal` | `NS_Healing_1` (набор курса) |
   | `Ability.Heal.Revive` | `NS_Healing_3` (набор курса) |
   | `Objective.Defuse.Progressed` | **`NS_DefuseSparks`** (своя, `XRU1Game/FX`) |
   | `Objective.Defuse.Completed` | **`NS_ChargeDisarmFlash`** (своя) |
   | `Objective.Evac.Unit` | **`NS_EvacFlare`** (своя) |

   Тройка «своих» систем — `NS_DefuseSparks`, `NS_ChargeDisarmFlash`,
   `NS_EvacFlare` в `Content/XRU1Game/FX` (созданы на базе набора
   `sA_PickupSet_1`, перенесённого из учебного проекта, — как и делалось в
   курсе); вид дошлифовывается в редакторе без правок кода. Разовые эффекты
   гаснут через `OneShotLifetimeSeconds`. Зональный дым эвакуации снят осознанно:
   дымоподобной системы в наборе нет, поле `EvacZoneSmokeEffect` и код ждут
   настоящего ассета.
2. **Фоновый звук окружения.** Критерий закрывается звуковой атмосферой боя:
   музыкальный слой (меню/хаб/бой + стингеры) и радио-эфир оператора (25 боевых
   реплик Купола через цепочку Source Effects «рация»: HPF 350 → LPF 3200 →
   BitCrusher 11 кГц/12 бит, `Audio/Effects/Chain_RadioVoice`) —
   [03_ARCHITECTURE.md](03_ARCHITECTURE.md) §12. Отдельный `AmbientSound`-актор с
   радиошумом на карте не стоит осознанно: постоянный шум не добавляет атмосферы и
   мешает репликам. Если приёмка курса потребует буквальный ambient-актор, он
   возвращается одной командой — ассет `radio-noise-interference` лежит в
   `Audio/Ambient/`.

## 6. ДЗ 5 — Локализация и многопоточность

**Критерии:** поддержка ещё одного языка через внутреннюю систему локализации;
обработка задач Системы квестов параллельно за счёт `AsyncTask`.

Задание разделяется на две независимые части, и их выгодно развести по проектам.

### 6а. Локализация → XRU1 — **ЗАКРЫТО, приёмка пройдена 2026-08-06**

Критерий «поддержка ещё одного языка через внутреннюю систему локализации» выполнен
штатным механизмом UE: цель `Game`, нативная культура `ru`, вторая `en`,
переключение в игре без перезапуска.

Что стоит за этим:

- `Config/Localization/Game_Gather.ini` и `Game_Compile.ini`;
- слой субтитров целиком (`UXRU1SubtitleSubsystem`, три режима жизни строки, титры
  интро, субтитры к озвучке через `USoundSubtitleData`) —
  [03_ARCHITECTURE.md](03_ARCHITECTURE.md) §13;
- в экране настроек секция «СУБТИТРЫ И ЯЗЫК» с выбором культуры;
- `Content/Localization/Game/` собран: **2050 слов**, обе культуры переведены
  целиком (0 пустых строк в архиве), `.locres` для `ru` и `en` скомпилированы.
  Это единственное место в документации, где записан охват локализации; остальные
  документы ссылаются сюда, а не повторяют число.

**Что здесь поучительного.** Текст, залитый в ассеты **скриптами** (дерево
обучения, реплики боя, названия и описания миссий, имена бойцов), сохраняется как
culture-invariant `FText` — без namespace и ключа. Для системы локализации такого
текста просто не существует: Gather его не пропускает, а **не видит**, и первая
сборка цели даёт около трети реального объёма при внешне исправных настройках.
Закрыто editor-утилитой `UXRU1LocalizationAuthoringLibrary`: она проходит по всем
`FText`-свойствам пакета (включая `FInstancedStruct` внутри `UStateTreeState`) и
назначает ключи детерминированно — 157 текстов сценариев и 17 имён бойцов. Разбор —
[03_ARCHITECTURE.md](03_ARCHITECTURE.md) §13, порядок работы —
[05_WORKFLOW.md](05_WORKFLOW.md) §3.1.1–3.1.2.

Локализация полезна и сама по себе: субтитры и озвучка — часть демо.

### 6б. Многопоточность → UE-HW

`AsyncTask`, `ParallelFor`, `FRunnable` и `Async(...)` в XRU1 не встречаются ни разу.
Плагин `STQuestSystem` в XRU1 при этом сильно ушёл вперёд относительно версии в
UE-HW (диалоги, `QuestSaveGame`, `QuestProgressStore`, `QuestTask_WaitOutcome`).
Вносить многопоточность в этот код перед защитой — лишний риск ради формального
критерия.

В UE-HW квестовая система проще и не критична. Если позже захочется поднять
изменения в XRU1 — плагин один и тот же, перенос возможен, но уже после сдачи.

## 7. Проектная работа (курсовая)

Критерии и чем закрыты — [01_GDD.md](01_GDD.md) §15. Кратко: жанр и референс; игровой
цикл ≥10 минут; отряд из 4 классов и противники; окружение из китов; главное меню,
пауза и настройки; завершающий экран; проходимость от начала до конца. Открытый
пункт — **исходники + билд**: упаковка Shipping-билда и тег версии выполняются
последним шагом, после финального прохождения.

## 8. Что осталось

Всё, что закрывается в XRU1, — **закрыто и принято прогонами**: ДЗ 3 (Landscape +
постпроцесс + PCG), ДЗ 4 (звук + свои VFX + радио-обработка), ДЗ 5а (локализация,
обе культуры по 2050 слов). ДЗ 1 и ДЗ 2 сданы в UE-HW.

Осталось ровно два пункта:

1. **UE-HW, ДЗ 5б** — `AsyncTask` в Системе квестов учебного проекта
   (`UE_Learning_TopDown`, свой экземпляр STQuestSystem — XRU1 не задевается).
2. **XRU1, упаковка билда** — в самом конце, после финального прохождения.
