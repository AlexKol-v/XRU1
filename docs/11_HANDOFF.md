# 11 — Состояние проекта и передача между машинами

> Обновлено: **2026-07-20**, коммит `c36582f`. Файл живёт до конца этапа 6;
> после чистого PIE-прогона обновить/удалить вместе с 08 и 10.

## Где мы сейчас

**Этап 6 (боевой HUD) — код готов, ждёт ручного PIE-прогона.**
Этапы 0–1 закрыты, 2–5 частично (см. `04_ROADMAP.md`).

Вся логика HUD и боя — в C++ (`Source/XRU1/Tactics`, `Source/XRU1/UI`).
В редакторе всё собрано и назначено; ассеты в гите (LFS).

## Что сделано в последней сессии (коммит c36582f)

1. **WBP_TacticalHUD вычищен** от логики, переехавшей в `UTacticalHUDWidget`
   (OnHoveredUnitChanged, RefreshButtons, AP-подписка, NewVar/BoundAPUnit).
2. **DA_TacticalHUDStyle** создан, заполнен, назначен; добавлен
   `ActionButtonPadding` — кнопки ActionsPanel одного размера.
3. **Фикс краша** «Accessed None … вне UClass»: `IsValid`-guard в обоих
   `ForEachLoop(Portraits)`.
4. **Атака по-XCOM'овски**: прямой ЛКМ по врагу не стреляет (только кнопка
   «Огонь» → клик), `EAttackTargetStatus` различает «слишком далеко» и «нет
   линии огня», AttackBtn гаснет без целей, `AttackRange` 3000/5000.
5. **Достижимость перемещения** — единственный источник правды — волновое поле
   (`AMoveRangeVisualizer::GetMoveCostTo`), см. `03_CODE_OVERVIEW.md §2.5`.
   Исправлен баг «клик за стену из 3 юнитов съедал ход».
6. **Архитектурный аудит** `Tactics/` — см. `03_CODE_OVERVIEW.md §2.4`.

## ЧТО ДЕЛАТЬ ДАЛЬШЕ (по приоритету)

### 1. Ручной PIE-прогон — БЛОКЕР приёмки этапа 6
Агент не может водить мышью в PIE. Чеклист: `09_TACTICAL_HUD_GUIDE.md §7`.
Особое внимание (свежие правки, не проверены живьём):
- [ ] кнопки ActionsPanel одного размера, не «прыгают» при нажатии;
- [ ] в Output Log нет спама `… вне UClass` (выбор бойца, смерть, стоп PIE);
- [ ] прямой ЛКМ по врагу НЕ стреляет; «Огонь» → клик по врагу стреляет;
- [ ] наведение на далёкого врага с чистой LOS → «Слишком далеко»
      (не «Нет линии огня»);
- [ ] **перекрыть проход 3 юнитами** → клик за них не проходит и НЕ ест AP;
- [ ] **одиночный юнит на пути в чистом поле** → приказ проходит, боец
      оббегает его (это регрессия-риск последней правки, проверить обязательно);
- [ ] враг, упёршийся в союзника, подходит насколько может и не теряет ход.

### 2. Хвосты этапа 6
- [ ] убрать диагностические логи `[Unit] …` в `AUnitBase`;
- [ ] после чистого прогона — удалить `08_HUD_TODO.md`, `10_SONNET_MCP_TASKS.md`,
      этот файл.

### 3. Дальше по роадмапу
Этап 4 (анимации ABP_Soldier, стойки укрытий) → этап 5 (способности классов) →
этап 7 (уровни). Подробности — `04_ROADMAP.md`.

### 4. Решить (ждёт вашего слова)
Предложение по подаче атаки P0–P3 — `04_ROADMAP.md`, этап 8: баннер режима
прицеливания, отмена в UI, кинематографичная камера выстрела, Tab по целям.
Не реализовано намеренно — это дизайн-решение, не баг.

## Переезд на другую машину

Пути НЕ хардкодить (см. `CLAUDE.md`): машина 1 — `D:/UE5/UnrealProjects/XRU1`,
машина 2 — `D:/Unrial_Projects/XRU1`; движок ищется через реестр.

```powershell
git pull
git lfs pull                      # бинарники ассетов
.\Build-XRU1.ps1                  # редактор должен быть ЗАКРЫТ
```
Затем открыть `XRU1.uproject`. Для MCP-моста к редактору (UnrealClaude):
`npm install` в `Plugins/UnrealClaude/Resources/mcp-bridge/` — если ещё не
делали на этой машине.

**codebase-memory индекс привязан к пути и на каждой машине свой** —
переиндексировать: `index_repository` с `repo_path` = корень проекта.
