# 14 — Анимации: что брать, а что достраивать

> **Статус (2026-07-26): подготовка ЗАВЕРШЕНА, сборка графа ABP — в работе.**
> Документ отвечает на один вопрос: достраивать существующий `ABP_Solider` или
> взять готовый проект/пак и не мучиться. Разбор сделан по официальной
> документации Epic и обсуждениям на форумах — источники в §5.
>
> Всё, что нужно было **перенести** (присед, cover-анимации, 4 оружия с
> текстурами) и **разложить по чистой структуре** — сделано, см.
> [15_EDITOR_TASKS.md](15_EDITOR_TASKS.md) блок E0. Ассеты нужно искать по
> актуальным путям `/Game/XRU1Game/Units/...`, а не по путям из этого раздела —
> они обновлены ниже. `ABP_Solider` уже создан и назначен всем пяти
> `BP_Unit_*`; внутри — пока нетронутый граф-заготовка (Idle/Walk/Run/Jump без
> оружия и укрытий) — это и есть текущий фронт работы, см. 15-й док блок E1
> шаги 4–11.

---

## 0. Короткий ответ

**Достраиваем свой ABP. Скачивать НИЧЕГО не надо — 90% анимаций уже лежит
в проекте.**

Конкретно:

1. **Систему движения не трогаем.** `ACharacter` + `CharacterMovementComponent`
   + `UCrowdFollowingComponent` + навмеш — на этом держатся зона хода,
   бюджет пути, диски занятости и весь AI. Любой «готовый проект» тянет за
   собой свою систему движения и ломает это целиком.
2. ⭐ **Анимации УЖЕ ЕСТЬ.** Проверено через редактор 2026-07-25: в проекте
   **97 `AnimSequence`** из шаблона UE5 Mannequin, включая полный **Rifle**-набор
   (idle ADS, fire, dry fire, equip, reload, aim-офсеты, walk/jog по 8
   направлениям), **8 hit-react** и **6 вариантов смерти**. См. §2 — таблицу
   «что есть / чего не хватает». Изначально этот документ рекомендовал тянуть
   GASP; после проверки содержимого проекта рекомендация ОТМЕНЕНА как лишняя
   работа.
3. **Схема ABP — гибрид**, стандартный для тактик: стейт-машина держит
   локомоцию и ПОЗУ укрытия, монтажи (через Default Slot) играют ДЕЙСТВИЯ —
   выстрел, привстать поверх укрытия, шаг за угол, реакция на попадание, смерть.
4. **Весь C++ уже написан** (2026-07-25, фаза S2): `FUnitVisualState`,
   `AUnitBase::GetVisualState()`, `GetFireMontageFor()`, слоты монтажей. В
   редакторе остаётся только собрать граф — §3.

---

## 1. Почему НЕ «взять готовый проект целиком»

| Вариант | Что даёт | Почему не подходит |
|---|---|---|
| **GASP как основа проекта** | 500+ анимаций, Motion Matching, мгновенно «дорого выглядит» | Построен вокруг Motion Matching + Chooser, а в 5.7 ещё и вокруг **плагина Mover**, который ЗАМЕНЯЕТ `CharacterMovementComponent`. У нас на CMC завязаны path following, Detour Crowd, диски занятости и вся зона хода. Это не «интеграция», это переписывание ядра движения |
| **Lyra** | продакшн-каркас, GAS уже внутри | Шутер от первого/третьего лица с сетевым кодом. Тактического движения по навмешу нет вообще, а объём чужого кода несопоставим с курсовой |
| **Платные паки «Rifle Shooter Pro» и подобные** | готовые cover/crouch/aim-наборы | Деньги + ассеты всё равно надо ретаргетить и разложить по стейтам. Ту же работу закрывает бесплатный GASP |
| **Достроить свой ABP + анимации из GASP** | контроль, ничего не ломается | Требует руками собрать стейт-машину и 6–8 монтажей |

⚠️ **Главная ловушка**, ради которой написан этот раздел: Motion Matching
решает задачу «естественно двигаться в произвольном направлении по вводу
игрока». У нас юнит идёт **по посчитанному маршруту навмеша, один за ход, и
камера смотрит сверху**. Задачи, которую решает Motion Matching, у нас нет —
а цена (замена системы движения) максимальная. Это тот случай, когда более
продвинутая технология объективно хуже подходит.

Это же подтверждает и практика: для пошаговых игр, где камера далеко, а
персонажи стилизованы, тонкость переходов локомоции почти не читается — вложение
в неё окупается хуже всего.

---

## 2. Инвентаризация: что УЖЕ в проекте ⭐

Проверено запросом к редактору: **AnimSequence** и меши лежат под единой
чистой структурой `/Game/XRU1Game/Units/` (реорганизация 2026-07-25/26,
подробности — [15_EDITOR_TASKS.md](15_EDITOR_TASKS.md) блок E0). Скелет — наш
же UE5 Manny (`Units/Meshes/SK_Mannequin`), ретаргет для Rifle/Death не нужен;
Crouch **отретаргечен** на него же, Cover/Revive играют через **Compatible
Skeletons** со своим `SK_Mannequin_AnimLib`.

| Нужно для тактики | Есть в проекте | Путь |
|---|---|---|
| Idle с винтовкой | ✅ `MF_Rifle_Idle_ADS` | `Units/Anim/Rifle/` |
| **Выстрел** | ✅ `MM_Rifle_Fire`, `MM_Rifle_DryFire` | `Units/Anim/Rifle/` |
| Перезарядка, экипировка | ✅ `MM_Rifle_Reload`, `MM_Rifle_Equip` | `Units/Anim/Rifle/` |
| Aim offsets (вверх/вниз/центр) | ✅ `MM_Rifle_Idle_ADS_AO_CU/CD/CC` | `Units/Anim/Rifle/AIM/` |
| Ходьба/бег с винтовкой, 8 направлений | ✅ `MF_Rifle_Walk_*`, `MF_Rifle_Jog_*` | `Units/Anim/Rifle/Walk`, `/Jog` |
| **Реакция на попадание** | ✅ **8 штук**: `MM_HitReact_Front_Lgt_01..04`, `Front_Med_01..02`, `Front_Hvy_01`, `Back_Med_01` | `Units/Anim/Rifle/HitReact/` |
| **Смерть** | ✅ **6 штук**: `MM_Death_Front_01..03`, `Back_01`, `Left_01`, `Right_01` | `Units/Anim/Death/` |
| Присед / поза низкого укрытия | ✅ перенесено + отретаргечено на наш скелет, **31 файл** (в т.ч. `BS_Crouch_new`, повороты, направленные лупы) | `Units/Anim/Crouch/` |
| **Прижаться к укрытию слева/справа** | ✅ `anim_CoverDown_Idle_Left/Right` | `Units/Anim/Cover/` |
| **Выглянуть из укрытия** | ✅ `anim_CoverDown_Look_Left/Right` | `Units/Anim/Cover/` |
| **Поза тяжелораненого + подъём медиком** | ✅ `anim_Downed_Idle_R`, `anim_Knocked_Reviver/Reviving`, `anim_Self_Revive_F_R` | `Units/Anim/Revive/` |
| **Меши оружия** (все 4 класса, с текстурами) | ✅ `SK_AssaultRifle_Frame`, `SK_SMG_Frame`, `SK_LightMachineGun_Frame`, `SK_Sniper_Frame` | `Units/Weapons/*/Meshes/` |
| `ABP_Solider` создан, назначен `Mesh → Anim Class` всем 5 `BP_Unit_*` | ✅ (граф внутри пока не переделан — заготовка Idle/Walk/Run/Jump без оружия) | `Units/Anim/ABP_Solider` |

**Вывод: не хватает НИЧЕГО материалом — всё нужное лежит в проекте.** Осталось
разложить по стейтам и собрать монтажи — см.
[15_EDITOR_TASKS.md](15_EDITOR_TASKS.md) §E1, шаги 2 и 4–11 (следующий фронт
работы).

> ⚠️ **Опечатка в имени:** ассет называется **`ABP_Solider`** (без второй «d»),
> а не `ABP_Soldier`, как этот документ называл его раньше. Переименовать
> можно (Content Browser → F2 → rename_asset поправит ссылки в 5 BP юнитов
> сам), но это чисто косметика — не обязательно перед сборкой графа.

⚠️ **Текстуры оружия сжаты, не удалены:** 4K-PBR текстуры перенесены полностью
(это была явная просьба — «с текстурами приятнее»), но выставлен
`Maximum Texture Size = 512` через Bulk Edit via Property Matrix — для
тактической камеры, где оружие занимает ~30 пикселей, здесь достаточно, а вес
в git-LFS меньше на порядок.

### Чем закрывали недостающее — история решения (актуально закрыто, раздел для памяти)

На момент первой версии этого документа (2026-07-25) не хватало crouch-набора
и cover-анимаций. Решили не тащить GASP/Fab-паки (см. §1, почему), а перенести
готовые анимации из проекта-донора `UE-HW` (присед, cover, revive) и бандл
оружия `ModernGunsBundle` (4 разных ствола с текстурами). Перенос выполнен,
пути разложены по чистой структуре — см. таблицу §2 выше и
[15_EDITOR_TASKS.md](15_EDITOR_TASKS.md) блок E0. GASP как источник анимаций
не понадобился.

⚠️ **Лицензию перенесённых ассетов проверить перед релизом/публикацией.**
Шаблонные анимации UE5 Manny вопросов не вызывают (часть движка); анимации из
`UE-HW`/`ModernGunsBundle` — по условиям соответствующего пака.

---

## 3. Целевая схема ABP (что собирать)

```
                 ┌─────────────── Anim Graph ───────────────┐
  Стейт-машина   │  Locomotion (Idle / Walk / Run)          │
  «ПОЗА»         │  Cover Idle: None / Half (присед) /      │
                 │              Full (вжаться), + сторона   │
                 └──────────────────┬───────────────────────┘
                                    │
                            [ Default Slot ]   ← сюда бьют МОНТАЖИ
                                    │
                                 Output Pose
```

**Стейт-машина держит ПОЗУ** (что юнит делает «постоянно»), **монтажи —
ДЕЙСТВИЕ** (что происходит один раз). Это и есть рекомендуемый гибрид: стейт-
машина отвечает за переходы состояний, а монтажи вставляются слотом для
разовых событий — выстрелов, реакций, смертей.

### Что уже готово в коде — ВЕСЬ функционал (2026-07-25, фаза S2 закрыта)

| Что зовём из BP | Что отдаёт | Зачем |
|---|---|---|
| **`GetVisualState()`** → `FUnitVisualState` | `Pose` (Stand/Moving/CrouchCover/HighCover/Overwatch/Hunkered/Downed/Dead), `Cover`, `CoverDirection` (мировое), **`CoverDirectionLocal`** (в осях юнита), `bMoving`, `bPlayerSide` | ЕДИНЫЙ вход стейт-машины. Раньше состояние надо было собирать из шести источников (теги ASC, флаги смерти, компонент укрытий, path following) — и они расходились |
| **`GetFireMontageFor(Target, OutStance, OutEye)`** | готовый `UAnimMontage*` + `EFiringStance` + точка, откуда реально идёт выстрел | BP не разбирает стойку сам: и монтаж, и геометрия берутся у того же источника, что решает бой |
| Слоты монтажей на `AUnitBase` | `FireMontageOpen`, `FireMontageOverCover`, `FireMontageStepOut`, `HitReactMontage`, `DeathMontage`, `OverwatchEnterMontage` | дизайнер назначает ассеты в BP юнита, C++ выбирает нужный |
| `OnShotFired`, `OnReactionShot`, `OnDied`, `OnDownedChanged`, `OnEvacuated`, `OnCoverStateChanged` | BP-хуки | точки запуска монтажей |
| `NotifyUnitStateChanged` | — | пересобирает `VisualState` и шлёт `OnUnitStateChanged`. Зовут все системы после изменения |

⚠️ **Приоритет позы совпадает с приоритетом иконки статуса в HUD**
(`GetStatusForUnit`): Dead → Downed → Hunkered → Overwatch → Moving → Full →
Half → Stand. Один порядок на иконку и на анимацию — иначе игрок видит в HUD
«глухая оборона», а в кадре бег.

### Минимальный набор монтажей (6 штук)

1. `AM_Fire_Open` — выстрел стоя.
2. `AM_Fire_OverCover` — привстать из-за низкого укрытия, выстрел, сесть.
3. `AM_Fire_StepOut` — шаг вбок за угол, выстрел, возврат.
   ⚠️ Сдвиг **визуальный, с возвратом**: позиция юнита, его укрытие и диск
   занятости остаются на настоящей клетке (инвариант §II.6 п.5 в 11-м доке).
4. `AM_Hit_React` — реакция на попадание.
5. `AM_Death` — смерть.
6. `AM_Overwatch_Enter` — вскинуть оружие, встать в наблюдение.

### Порядок работ

| Шаг | Что | Предусловие |
|---|---|---|
| 1 | **S2 — `FUnitVisualState`** (13-й док): единая точка состояния для ABP вместо шести разбросанных источников | — |
| 2 | Перенос анимаций из GASP + IK Retarget на нашего Manny | — |
| 3 | Стейт-машина позы: локомоция + 3 позы укрытия + сторона стены | S1 ✅ уже сделан |
| 4 | Монтажи 1–3 (выстрел по `EFiringStance`) — это **Ф10** в 11-м доке | шаг 1 |
| 5 | Монтажи 4–6 (реакция, смерть, овервотч) | шаг 1 |
| 6 | **Ф11** — превью точки выглядывания игроку | Ф10 |

---

## 4. Приёмка

- У низкого укрытия боец **приседает**, при выстреле привстаёт и садится обратно.
- У высокого — **вжимается в стену нужной стороной** (`BestCoverDirection`),
  при выстреле выходит за край и **возвращается**.
- В чистом поле — обычная стойка и обычный выстрел.
- Наблюдение читается позой, а не только иконкой.
- ⚠️ Проверить, что визуальный сдвиг `StepOut` **не сдвигает** реальную
  позицию: укрытие, щит в HUD и диск занятости обязаны остаться прежними.

---

## 5. Источники

Проверялись 2026-07-25.

1. **Game Animation Sample Project, документация Epic** — состав, скелет
   UEFN_Mannequin, требование Motion Matching/Chooser, перенос через
   Migrate/Export, ретаргет через IK Retargeter:
   [dev.epicgames.com — Game Animation Sample Project](https://dev.epicgames.com/documentation/en-us/unreal-engine/game-animation-sample-project-in-unreal-engine)
2. **Epic, анонс проекта** — «500+ game-ready анимаций, рантайм-ретаргет на
   скелет UE5 Mannequin»:
   [unrealengine.com/blog/game-animation-sample](https://www.unrealengine.com/blog/game-animation-sample)
3. **Epic, обновление GASP в 5.7** — переход на плагин **Mover**, smart objects,
   locomotion style (главный аргумент «не брать как основу»):
   [unrealengine.com/tech-blog — updates to GASP in UE 5.7](https://www.unrealengine.com/tech-blog/explore-the-updates-to-the-game-animation-sample-project-in-ue-5-7)
4. **Motion Matching, документация** — что это и для какой задачи:
   [dev.epicgames.com — Motion Matching](https://dev.epicgames.com/documentation/unreal-engine/motion-matching-in-unreal-engine)
5. **State Machines, документация** — стейт-машина как инструмент состояний
   движения:
   [dev.epicgames.com — State Machines](https://dev.epicgames.com/documentation/en-us/unreal-engine/state-machines-in-unreal-engine)
6. **Форумы Epic, «State Machine vs Anim Montage»** — гибрид «стейт-машина +
   Default Slot для монтажей» как рекомендуемая практика:
   [forums.unrealengine.com — advice on state machine versus montage](https://forums.unrealengine.com/t/advice-on-when-to-choose-to-use-a-state-machine-versus-an-animation-montage-help/138994),
   [forums.unrealengine.com — when to use State Machine or Anim Montage](https://forums.unrealengine.com/t/when-to-use-state-machine-or-anim-montage/138686)
